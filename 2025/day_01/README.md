to compile c code

```sh compile a.c```

to run it

```cat input | ./a.c.exec```

to run awk code

```awk -f a.awk input```

to run prolog code

```gprolog
[b].
main.

```

to run forth code
```
awk -f ../../language/forth.awk a.fs
```
```
awk -f ../../language/forth.awk b.fs
```